import React from "react";
import styles from "../app.css";
const Info = () => (
  <section className="info">
        <h2 className="info__title">Weather now </h2>
        </section>
);
export default Info;